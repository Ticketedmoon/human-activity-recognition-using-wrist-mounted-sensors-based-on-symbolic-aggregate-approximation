#### This directory will host any generator/build scripts for graphs/visualisations.  
In order to run the scripts, all dependencies must be installed from the requirements.txt  
in the previous directory. 
